{
  "name": "secrets-detector",
  "description": "Local secrets detection for hardcoded API keys and leaked tokens",
  "type": "stdio",
  "command": "secrets-detector-mcp-server",
  "args": [],
  "env": {},
  "notes": {
    "installation": "Install secrets-detector-mcp-server",
    "capabilities": [
      "Detect hardcoded API keys",
      "Find leaked tokens in logs",
      "Identify credentials in configs",
      "Pattern-based secret scanning"
    ],
    "privacy": "Strictly local - no data sent externally"
  }
}
