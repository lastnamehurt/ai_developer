# docs: refresh README/help and add architecture overview

## Summary
- Update README to reflect v1 features (quickstart, profiles, TUI, MCP browse, env mgmt).
- Ensure `ai --help` output aligns with current commands/flags.
- Add architecture overview/diagram and demo script pointers.

## Acceptance Criteria
- README shows curl install, quickstart, profile usage, env commands, MCP browse/test, and TUI.
- `ai --help`/`ai help` matches implemented commands and examples.
- Architecture overview doc or diagram committed and linked from README.

## Tasks
- Rewrite README sections for Quickstart/Profiles/MCP/Env/Error handling.
- Sync CLI help text with new commands.
- Add architecture overview (could be a markdown doc + optional diagram asset).
