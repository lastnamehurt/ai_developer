# Git Configuration (for AI tools to make commits)
GIT_AUTHOR_NAME="Your Name"
GIT_AUTHOR_EMAIL="your-email@company.com"

# GitLab Configuration
GITLAB_PERSONAL_ACCESS_TOKEN=your-gitlab-token-here
GITLAB_URL=https://gitlab.example.com
GITLAB_API_URL=https://gitlab.example.com/api/v4

# Memory Bank MCP Configuration
MEMORY_BANK_ROOT="${HOME}/.local/ai-dev/memory-banks"

# Kubernetes Configuration (optional - defaults to ~/.kube/config)
KUBECONFIG=~/.kube/config
